<Page
    x:Class="UwpWebApps.AppsHubPageFrames.AboutFrame"
    x:Name="aboutFrames"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpWebApps.AppsHubPageFrames"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.TopAppBar>
        <CommandBar Background="Transparent" Margin="150,0,0,0" DefaultLabelPosition="Right">
            <AppBarButton Name="feedbackButton" Label="Feedback" Click="feedbackAppButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE939;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Page.TopAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel DataContext="{Binding ElementName=aboutFrames}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-50,0,0">
            <TextBlock Text="{Binding Path=AppName}" Foreground="DodgerBlue" FontSize="30" Margin="0,0,0,10" />
            <TextBlock Text="{Binding Path=AppPublisher}" />
            <TextBlock Text="{Binding Path=AppVersion}" />
        </StackPanel>
    </Grid>
</Page>
